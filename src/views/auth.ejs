<!DOCTYPE html>
<html lang="en">
  <%-include('./partials/head.ejs') %>
  <body>
    <div class="w-full min-h-screen flex flex-col justify-center items-center">
      <div id="btnBack" class="absolute top-0 left-0 m-5">
        <i
          class="
            far
            fa-arrow-alt-circle-left
            text-5xl
            cursor-pointer
            text-black
            opacity-60
            hover:opacity-100 hover:text-red-500
            transition-all
          "
        ></i>
      </div>
      <section
        class="max-w-xl w-full bg-gray-100 p-10 rounded-2xl shadow-inner"
      >
        <h1
          id="authFormTitle"
          class="w-full p-3 text-center text-2xl font-semibold uppercase"
        >
          로그인
        </h1>
        <form
          id="authForm"
          autocomplete="off"
          action="<%= routes.login %>"
          method="POST"
          class="flex flex-col w-full"
        >
          <input
            class="
              p-3
              outline-none
              mb-3
              shadow-inner
              border border-gray-300
              rounded-2xl
            "
            type="email"
            name="email"
            required
            placeholder="이메일"
          />
          <input
            class="
              p-3
              outline-none
              shadow-inner
              rounded-2xl
              border border-gray-300
            "
            type="password"
            name="password"
            required
            placeholder="비밀번호"
          />
          <button
            type="submit"
            class="
              mt-5
              text-center
              p-3
              bg-red-400
              w-full
              rounded-2xl
              text-white
              hover:scale-105
              transform
              transition-all
              shadow-inner
            "
          >
            <span id="authFormSpan">로그인</span>
          </button>
        </form>
        <div class="w-full pt-5 border-b border-gray-300"></div>
        <button
          id="btn-modeChange"
          class="
            mt-3
            p-3
            border border-gray-300
            bg-green-500
            text-white
            w-full
            rounded-2xl
            hover:scale-105
            transform
            transition-all
            shadow-inner
            mb-5
          "
        >
          회원가입하기
        </button>
      </section>
    </div>

    <script src="/src/client/js/auth.js"></script>
  </body>
</html>
