<!DOCTYPE html>
<html lang="en">
  <%-include('./partials/head.ejs') %>
  <body>
    <%-include('./partials/header.ejs') %>
    <main class="w-full min-h-screen flex justify-center items-center pb-20">
      <div
        class="
          p-10
          max-w-xl
          bg-gray-200
          w-full
          mx-auto
          rounded-2xl
          shadow-inner
          text-center
        "
      >
        <% if (user) { %>
        <h1>반갑습니다 <%= user.email %></h1>
        <% } else { %>
        <h1 class="font-medium">유저 정보가 없습니다. 로그인 해주세요.</h1>
        <% } %>
      </div>
    </main>

    <script src="/src/client/js/home.js"></script>
  </body>
</html>
